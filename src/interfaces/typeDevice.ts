export interface TypesDeviceProps {
    typeDevices:{
        id: number;
        name: string;
    }[]
}

export interface TypeDeviceProps {
    typeDevices:{
        id: number;
        name: string;
    }

}

export interface TypeDeviceBody {
    name: string
}